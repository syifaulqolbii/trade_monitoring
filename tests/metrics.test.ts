import { describe,it,expect } from "vitest";
import { computeMetrics,growthDrawdownSeries,type DealInput,type SnapshotInput } from "@/lib/metrics";
const d=(s:string)=>new Date(s);
const cash=(profit:number,time:string):DealInput=>({ticket:time,positionId:"0",symbol:"",type:2,direction:0,volume:0,price:0,profit,commission:null,swap:null,fee:null,time:d(time)});
describe("cash-flow-adjusted growth",()=>{it("deposit tidak menjadi growth",()=>{const snaps:SnapshotInput[]=[{balance:40000,equity:40000,createdAt:d("2026-01-01T00:00:00Z")},{balance:50000,equity:50000,createdAt:d("2026-01-02T00:00:00Z")}];const m=computeMetrics([cash(10000,"2026-01-01T12:00:00Z")],[],snaps);expect(m.growthPct).toBeCloseTo(0,5);expect(m.maxDrawdownPct).toBeCloseTo(0,5)});it("withdrawal tidak menjadi drawdown",()=>{const snaps:SnapshotInput[]=[{balance:50000,equity:50000,createdAt:d("2026-01-01T00:00:00Z")},{balance:40000,equity:40000,createdAt:d("2026-01-02T00:00:00Z")}];const m=computeMetrics([cash(-10000,"2026-01-01T12:00:00Z")],[],snaps);expect(m.growthPct).toBeCloseTo(0,5);expect(m.maxDrawdownPct).toBeCloseTo(0,5)});it("kurva growth mengecualikan deposit",()=>{const snaps:SnapshotInput[]=[{balance:40000,equity:40000,createdAt:d("2026-01-01T00:00:00Z")},{balance:50000,equity:50000,createdAt:d("2026-01-02T00:00:00Z")}];const series=growthDrawdownSeries(snaps,[cash(10000,"2026-01-01T12:00:00Z")]);expect(series.at(-1)?.growthPct).toBeCloseTo(0,5)})});
